import { extendTheme } from "@chakra-ui/react";

const theme = extendTheme({
  colors: {
    primary: "#F7F7F9",
    secondary: "#322659",
  },

  fonts: {
    heading: "Nunito",
    body: "Nunito",
  },
});

export default theme;
